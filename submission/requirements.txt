# Python deps to run submission/ccdp_submission.ipynb locally.
#   pip install -r submission/requirements.txt
# Equivalent to `pip install -e .[ml]` from the repo root, plus notebook extras.
# Python 3.11+ recommended.

# core
pyyaml>=6.0
requests>=2.31
typer>=0.12
rich>=13.7
pydantic>=2.6
jinja2>=3.1

# ML
torch>=2.2
torchvision>=0.17
ultralytics>=8.1
xgboost>=2.0
scikit-learn>=1.4
pandas>=2.2
numpy>=1.26
pillow>=10.2
opencv-python-headless>=4.9
scipy>=1.10

# notebook
jupyter>=1.0
ipywidgets>=8.1
matplotlib>=3.8
certifi>=2024.2.2

# interactive UI demo (§13 — Gradio app launched from the notebook)
gradio>=5,<6
python-multipart>=0.0.9
# Gradio's pydub needs audioop, removed from the stdlib in Python 3.13+.
audioop-lts>=0.2.1; python_version >= "3.13"

# dataset fetch (optional — only needed for RUN_FETCH_DATA / RUN_TRAINING)
kagglehub>=0.2
torchinfo>=1.8
